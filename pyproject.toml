[project]
name = "frameless-pyside6-template"
version = "0.1.0"
description = "Reusable cross-platform frameless PySide6/QML desktop template"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.6,<6.12",
    "cryptography>=42",
]

[tool.ruff]
line-length = 100
